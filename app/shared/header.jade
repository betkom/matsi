nav.navbar.navbar-static-top(role='navigation')
  div.container
    div.navbar-header
      button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-ex1-collapse')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/') Matsi
    div.collapse.navbar-collapse.navbar-right.navbar-ex1-collapse
      ul.nav.navbar-nav
        li
          a(href='mentors') View Mentors
        li
          a(href='fellows') View Fellows
        li.active
          a(href='myProfile') My Profile
        li
          p
            a.bdr(ng-hide="currentUser" ng-click="login('google')") login
            p(ng-if="allowUser") Account disabled, contact the Administrator.
          div(ng-show="currentUser")
            p
              | {{currentUser.email }}&nbsp;
              a.bdr(ng-click="logout()") sign out